import 'package:flutter/material.dart';

const kTextFieldDecoration = InputDecoration(
  hintText: 'Enter a value',
  hintStyle: TextStyle(color: Colors.black),
  contentPadding: EdgeInsets.symmetric(vertical: 20.0, horizontal: 20.0),
  border: OutlineInputBorder(
    borderRadius: BorderRadius.all(Radius.circular(7)),
  ),
  enabledBorder: OutlineInputBorder(
    borderSide: BorderSide(color: Colors.black, width: 1.0),
    borderRadius: BorderRadius.all(Radius.circular(7.0)),
  ),
  focusedBorder: OutlineInputBorder(
    borderSide: BorderSide(color: Colors.black, width: 1.5),
    borderRadius: BorderRadius.all(Radius.circular(7)),
  ),
);

const pColor = Color(0xFF9be2de);

const sColor = Color(0xFFffffff);

const tColor = Color(0xFF8b8c8e);

const ttColor = Color(0xFFd0d1d1);

const yColor = Color(0xFFf3f3f3);

const aColor = Color(0xFFff0000);

const wColor = Color(0xFFFFC107);

const bColor = Color(0xFF000000);

const gColor = Color(0xFF124243);

const boxSize = 20.0;
